/* This file is autogenerated by mkstatements.pl from dbh_statements.in */

const char *dbh_stmt_sql[] = {
    "pragma user_version",
    "insert into file (archive_id, file_type, file_idx, detector_id, location, size, crc, md5, sha1) values (?, ?, ?, ?, ?, ?, ?, ?, ?)",
    "select archive_id, file_idx, detector_id, location from file f where file_type = ? order by location",
    "select archive_id, file_idx, detector_id, location from file f where file_type = ? and (f.crc = ? or f.crc is null) order by location",
    "select archive_id, file_idx, detector_id, location from file f where file_type = ? and (f.md5 = ? or f.md5 is null) order by location",
    "select archive_id, file_idx, detector_id, location from file f where file_type = ? and (f.crc = ? or f.crc is null) and (f.md5 = ? or f.md5 is null) order by location",
    "select archive_id, file_idx, detector_id, location from file f where file_type = ? and (f.sha1 = ? or f.sha1 is null) order by location",
    "select archive_id, file_idx, detector_id, location from file f where file_type = ? and (f.crc = ? or f.crc is null) and (f.sha1 = ? or f.sha1 is null) order by location",
    "select archive_id, file_idx, detector_id, location from file f where file_type = ? and (f.md5 = ? or f.md5 is null) and (f.sha1 = ? or f.sha1 is null) order by location",
    "select archive_id, file_idx, detector_id, location from file f where file_type = ? and (f.crc = ? or f.crc is null) and (f.md5 = ? or f.md5 is null) and (f.sha1 = ? or f.sha1 is null) order by location",
    "select archive_id, file_idx, detector_id, location from file f where file_type = ? and size = ? order by location",
    "select archive_id, file_idx, detector_id, location from file f where file_type = ? and size = ? and (f.crc = ? or f.crc is null) order by location",
    "select archive_id, file_idx, detector_id, location from file f where file_type = ? and size = ? and (f.md5 = ? or f.md5 is null) order by location",
    "select archive_id, file_idx, detector_id, location from file f where file_type = ? and size = ? and (f.crc = ? or f.crc is null) and (f.md5 = ? or f.md5 is null) order by location",
    "select archive_id, file_idx, detector_id, location from file f where file_type = ? and size = ? and (f.sha1 = ? or f.sha1 is null) order by location",
    "select archive_id, file_idx, detector_id, location from file f where file_type = ? and size = ? and (f.crc = ? or f.crc is null) and (f.sha1 = ? or f.sha1 is null) order by location",
    "select archive_id, file_idx, detector_id, location from file f where file_type = ? and size = ? and (f.md5 = ? or f.md5 is null) and (f.sha1 = ? or f.sha1 is null) order by location",
    "select archive_id, file_idx, detector_id, location from file f where file_type = ? and size = ? and (f.crc = ? or f.crc is null) and (f.md5 = ? or f.md5 is null) and (f.sha1 = ? or f.sha1 is null) order by location",
    "update file set crc = ?, md5 = ?, sha1 = ? where archive_id = ? and file_type = ? and file_idx = ? and detector_id = ?",
    "delete from file where archive_id = ? and file_type = ? and file_idx = ?",
    "update file set file_idx=file_idx-1 where archive_id = ? and file_type = ? and file_idx > ?",
    "select name, description, version from dat where dat_idx >= 0 order by dat_idx",
    "select g.name, f.file_idx from game g, file f where f.game_id = g.game_id and f.file_type = ? and f.status <> ?",
    "select g.name, f.file_idx from game g, file f where f.game_id = g.game_id and f.file_type = ? and f.status <> ? and (f.crc = ? or f.crc is null)",
    "select g.name, f.file_idx from game g, file f where f.game_id = g.game_id and f.file_type = ? and f.status <> ? and (f.md5 = ? or f.md5 is null)",
    "select g.name, f.file_idx from game g, file f where f.game_id = g.game_id and f.file_type = ? and f.status <> ? and (f.crc = ? or f.crc is null) and (f.md5 = ? or f.md5 is null)",
    "select g.name, f.file_idx from game g, file f where f.game_id = g.game_id and f.file_type = ? and f.status <> ? and (f.sha1 = ? or f.sha1 is null)",
    "select g.name, f.file_idx from game g, file f where f.game_id = g.game_id and f.file_type = ? and f.status <> ? and (f.crc = ? or f.crc is null) and (f.sha1 = ? or f.sha1 is null)",
    "select g.name, f.file_idx from game g, file f where f.game_id = g.game_id and f.file_type = ? and f.status <> ? and (f.md5 = ? or f.md5 is null) and (f.sha1 = ? or f.sha1 is null)",
    "select g.name, f.file_idx from game g, file f where f.game_id = g.game_id and f.file_type = ? and f.status <> ? and (f.crc = ? or f.crc is null) and (f.md5 = ? or f.md5 is null) and (f.sha1 = ? or f.sha1 is null)",
    "select g.name, f.file_idx from game g, file f where f.game_id = g.game_id and f.file_type = ? and f.name = ?",
    "select file_idx from file where file_type = 1 limit 1",
    "select rule_idx, start_offset, end_offset, operation from rule order by rule_idx",
    "select type, offset, size, mask, value, result from test where rule_idx = ? order by test_idx",
    "select distinct name from file where file_type = 1 order by name",
    "select name from game order by name",
    "delete from file where game_id = ?",
    "delete from game where game_id = ?",
    "insert into dat (dat_idx, name, author, version) values (-1, ?, ?, ?)",
    "insert into dat (dat_idx, name, description, version) values (?, ?, ?, ?)",
    "insert into file (game_id, file_type, file_idx, name, merge, status, location, size, crc, md5, sha1) values (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)",
    "insert into game (name, description, dat_idx, parent) values (?, ?, ?, ?)",
    "insert into rule (rule_idx, start_offset, end_offset, operation) values (?, ?, ?, ?)",
    "insert into test (rule_idx, test_idx, type, offset, size, mask, value, result) values (?, ?, ?, ?, ?, ?, ?, ?)",
    "select name from game where parent = ?",
    "select name, author, version from dat where dat_idx = -1",
    "select name, merge, status, location, size, crc, md5, sha1 from file where game_id = ? and file_type = ? order by file_idx",
    "select game_id, description, dat_idx, parent from game where name = ?",
    "select game_id from game where name = ?",
    "select name from file where file_type = ? and crc not null limit 1",
    "select name from file where file_type = ? and md5 not null limit 1",
    "select name from file where file_type = ? and sha1 not null limit 1",
    "select parent from game where game_id = ?",
    "select parent from game where name = ?",
    "select file_type, count(name), sum(size) from file group by file_type order by file_type",
    "select count(name) from game",
    "update file set location = ? where game_id = ? and file_type = ? and file_idx = ?",
    "update game set parent = ? where game_id = ?",
    "insert into archive (name, mtime, size) values (?, ?, ?)",
    "insert into archive (name, archive_id, mtime, size) values (?, ?, ?, ?)",
    "select name from archive",
    "select mtime, size from archive where archive_id = ?",
    "select archive_id from archive where name = ?",
    "delete from archive where archive_id = ?",
    "select count(archive_id) from archive",
    "insert into file (archive_id, file_idx, detector_id, name, mtime, status, size, crc, md5, sha1) values (?, ?, ?, ?, ?, ?, ?, ?, ?, ?)",
    "select file_idx, detector_id, name, mtime, status, size, crc, md5, sha1 from file where archive_id = ? order by file_idx, detector_id",
    "delete from file where archive_id = ?",
    "select detector_id, name, version from detector",
    "insert into detector (detector_id, name, version) values (?, ?, ?)",
};
